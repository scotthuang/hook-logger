{
  "name": "@scotthuang/hook-logger",
  "version": "1.0.4",
  "description": "Log all hook stages for debugging in OpenClaw",
  "main": "index.ts",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
